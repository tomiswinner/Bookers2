<!DOCTYPE html>
<html>
  <head>
    <title>Booker2</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/ce3ce14c78.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-xl bg-dark navbar-dark">
        <a class="navbar-brand" href="/">Bookers</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggler1" aria-controls="navbarToggler1">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-lg-end mx-lg-5" id="navbarToggler1">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item mr-4 my-2">
                <%= link_to(user_path(current_user.id), class:"navbar-link text-white font-weight-bold") do %>
                  <i class="fas text-white fa-home"></i> Home</a>
                <% end %>
              </li>
              <li class="nav-item mr-4 my-2">
                <i class="fas text-white fa-users"></i> <%= link_to("Users",users_path,class: "navbar-link text-white font-weight-bold") %>
              </li>
              <li class="nav-item mr-4 my-2">
                <i class="fas text-white fa-book-open"></i> <%= link_to("Books",books_path,class: "navbar-link text-white font-weight-bold") %>
              </li>
              <li class="nav-item mr-4 my-2">
                <i class="fas text-white fa-sign-out-alt"></i> <%= link_to("Log out",destroy_user_session_path,method: :delete,class: "navbar-link text-white font-weight-bold") %>
              </li>
            <% else %>
              <li class="nav-item mr-4 my-2">
                <a class="navbar-link text-white font-weight-bold" href="/"><i class="fas text-white fa-home"></i> Home</a>
              </li>
              <li class="nav-item mr-4 my-2">
                <i class="fas text-white fa-link"></i> <%= link_to("About",about_path,class: "navbar-link text-white font-weight-bold") %>
              </li>
              <li class="nav-item mr-4 my-2">
                <i class="fas text-white fa-user-plus"></i> <%= link_to("Sign up",new_user_registration_path,class: "navbar-link text-white font-weight-bold") %>
              </li>
              <li class="nav-item mr-4 my-2">
                <i class="fas text-white fa-sign-in-alt"></i> <%= link_to("Log in ",new_user_session_path,class: "navbar-link text-white font-weight-bold") %>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    
    <div class="container-fluid">
      <div class="row">
        <div class="col">
          <% flash.each do |type, msg| %>
            <% if type == "notice" %>
              <div class="alert alert-success">
            <% elsif type == "alert" %>
              <div class="alert alert-danger">
            <% end %>
                <%= h(msg).gsub(/\n/,"<br>").html_safe %>
              </div>
          <% end %>
        </div>
      </div>
    </div>
    
    <div class="container-xl px-3">
      <%= yield %>
    </div>
    
    <footer class="mt-3">
      <div class="container-fluid">
        <div class="row justify-content-center">
          <p>
            CopyRight Infratop.inc
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>
